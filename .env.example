# Atlas Hookah — environment variables example
# Copy to .env and adjust as needed.

# Prisma / SQLite connection (relative to project root)
DATABASE_URL="file:./db/atlas-hookah.db"

# Node environment
NODE_ENV="development"
